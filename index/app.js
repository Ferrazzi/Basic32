// Manifest fisso + console dopo il flash
const installer = document.getElementById('installer');
installer.setAttribute('manifest', 'manifests/firmware.json');
installer.setAttribute('show-console', '');
